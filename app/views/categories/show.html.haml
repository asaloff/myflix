%section.genre
  %header.clearfix
    %h1= @category.title
  %article.row
    - @category.recent_videos.each do |video|
      .video.col-md-2
        = link_to video_path(video), nil do 
          =image_tag video.small_cover_url